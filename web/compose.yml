services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: miks_web
    environment:
      - NODE_ENV=production
      - API_URL=http://api:3000
    networks:
      - miks_net
      - dokploy-network
    restart: unless-stopped